#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int arr[n*n + 1];
    int arr1[n*n + 1];
    for (size_t i = 1; i <= n*n + 1; i++)
    {
        cin >> arr[i];
    }
    sort(arr+1,arr+n*n+2);
    int q;
    cin >> q;
    for (size_t i = 0 ;i <= q; i++)
    {
        int x,k;
        cin >> x >> k;
        
    }
    
}